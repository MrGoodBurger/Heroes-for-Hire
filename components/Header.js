//The is the Header that should have two icons show notifications and redirects to the Chat and Account pages

//Features
//Chat Icon - INCOMPLETE
//Account Icon - INCOMPLETE

import React from "react";
import { ScrollView } from "react-native";

const Header = () => {
    return(
       <View>
           
       </View>

    )
};

export default Header;